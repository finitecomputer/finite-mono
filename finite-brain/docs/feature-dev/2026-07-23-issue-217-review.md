# Issue #217 review packet

## Issue

- Issue: `finitecomputer/finite-mono#217`
- Slice type: server + CLI tracer
- Acceptance criteria: issue #217
- Baseline: `894d1ba`
- Current diff: `894d1ba...317029e`

## Implementation Summary

The initial slice adds a signed desired-state Organization Brain Collaboration
route, transactional store convergence, and `fbrain collaborators
ensure-admin`. Independent review found that the interface shape is useful but
the implementation and evidence are not yet sufficient to accept the ticket.

## Implementation Evidence

- `implement` session: `/root/ticket_217_worker`; review-fix session:
  `/root/ticket_217_fixes`
- `tdd` seams: signed HTTP route, transactional store, public CLI, and built
  process acceptance
- Behavioral tests: store convergence/audit/idempotency, route alias and
  cardinality rejection, typed HTTP rejection, CLI help, and two independent
  Finite Homes reading restricted content
- Green implementation: `317029e`
- Commands run: formatting, clippy, component suites, and built-process
  acceptance recorded in the issue session

## Review Instructions

Review only this issue's slice unless a severe cross-slice regression appears.
Keep standards and spec findings separate.

Check:

- Acceptance criteria are met.
- Tests verify behavior through public interfaces.
- No implementation-only tests masquerade as behavior tests.
- No incomplete work, placeholders, or unrelated changes remain.
- Relevant tests, checks, builds, and process acceptance pass.

## Reviewer Output

```text
STANDARDS_STATUS: addressed
STANDARDS_FINDINGS:
- Transport uncertainty is now distinct from typed authoritative HTTP status
  rejection; only transport errors produce an indeterminate CLI receipt.
- Folder/grant fanout is bounded at the route and the transaction carries
  Folder-scoped access-change evidence alongside each grant.
- The unsupported compatibility route and CLI alias were removed.
- The command is present in CLI help and the packaged CLI reference.
- Focused CLI/server behavior suites and the built-process acceptance pass.

SPEC_STATUS: addressed
SPEC_FINDINGS:
- Receipts now derive completion from fresh authoritative role, current-grant,
  and Folder-inventory proof, including added/deleted snapshot drift.
- Partial CLI output gives actionable key-holder retry guidance.
- Session evidence records the actual focused tests and leaves process
  acceptance explicitly pending rather than overstating it.
- The required built-process, two-Finite-Home acceptance now passes against a
  real signed Brain server and proves recipient access to restricted content.
```
