# Issue #217 review packet

## Issue

- Issue: `finitecomputer/finite-mono#217`
- Slice type: server + CLI tracer
- Acceptance criteria: issue #217
- Baseline: `894d1ba`
- Current diff: `894d1ba...HEAD` (final round-4 correction commit reported in
  the handoff)

## Implementation Summary

The slice adds a signed desired-state Organization Brain Collaboration route,
transactional store convergence, and `fbrain collaborators ensure-admin`.
Three independent review rounds identified correctness and evidence gaps; four
worker passes now address them at the public signed-route, public CLI, and
built-process seams.

## Implementation Evidence

- `implement` sessions: `/root/ticket_217_worker`,
  `/root/ticket_217_fixes`, `/root/ticket_217_fixes_round3`, and
  `/root/ticket_217_fixes_round4`
- `tdd` seams: signed HTTP route, transactional store, public CLI, and built
  process acceptance
- Behavioral tests: store convergence/audit/idempotency; full signed-route
  convergence, drift, rejection, rollback, secrecy, holder-identity, and
  cardinality behavior; malformed-success CLI uncertainty; email-first human
  rendering; and two independent Finite Homes reading restricted content
- Green implementation: `49c8b66` plus the final round-4 correction commit
  reported in the handoff
- Commands run: formatting, checks, clippy, full store/server/CLI suites, and
  built-process acceptance recorded in the issue session

## Review Instructions

Review only this issue's slice unless a severe cross-slice regression appears.
Keep standards and spec findings separate.

Check:

- Acceptance criteria are met.
- Tests verify behavior through public interfaces.
- No implementation-only tests masquerade as behavior tests.
- No incomplete work, placeholders, or unrelated changes remain.
- Relevant tests, checks, builds, and process acceptance pass.

## Reviewer Output

```text
STANDARDS_STATUS: ready_for_independent_round_4_review
STANDARDS_FINDINGS:
- Round 1 found collapsed HTTP errors, ignored authoritative postconditions,
  missing behavioral tests and bounds, an unsupported alias, and incomplete
  help/retry guidance; `bb500b7` addressed those findings.
- Round 2 found loss of authoritative non-2xx status on body-read failure and
  insufficient valid signed-route coverage; `49c8b66` addressed those
  findings.
- Round 3 found issuer-derived holder guidance, raw-npub human fallback, a
  global body limit smaller than the valid batch contract, and remaining
  signed-route/HTTP-backed evidence gaps; the final round-4 correction
  addresses those findings.

SPEC_STATUS: ready_for_independent_round_4_review
SPEC_FINDINGS:
- Round 2 found all-or-nothing local grant opening, malformed-success response
  misclassification, lost partial reasons, missing current-holder identity,
  and an npub-first process fixture; `49c8b66` addressed those findings.
- Round 3 found that holder identities still represented grant issuers rather
  than recipients, human output could expose an npub, valid maximum batches
  could not cross the HTTP boundary, and several acceptance scenarios lacked
  public-boundary proof; the final round-4 correction addresses those
  findings.
- Current evidence proves authoritative complete/partial semantics, repair and
  retry convergence, recipient-derived holder guidance, secret-free receipts,
  the full documented batch size, and the real two-Finite-Home signed-server
  acceptance.
```
