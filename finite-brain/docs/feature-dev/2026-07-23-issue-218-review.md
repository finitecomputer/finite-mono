# Review Packet

## Issue

- Issue: https://github.com/finitecomputer/finite-mono/issues/218
- Slice type: Product Client access truth and repair tracer
- Acceptance criteria:
  - Brain Role and ready/total Folder state are separate facts.
  - Canonical email addition uses the desired-state collaboration operation.
  - Complete, partial, and indeterminate receipts are accessible and safe.
  - Partial state names unready Folders and offers same-intent repair.
  - Existing incomplete admins are visible on initial load.
  - Product Client and existing interaction/access suites remain green.
- Baseline: `cc9dfa4`
- Current diff: `git diff cc9dfa4 -- finite-brain/crates/finite-brain-server finite-brain/docs/feature-dev`

## Implementation Summary

Organization Brain admins now see authoritative current Folder Key Grant
coverage beside each collaborator's separate Brain Role. Adding or repairing
an administrator uses the exact #217 desired-state endpoint and client-side
grant preparation. Partial and uncertain outcomes remain explicit and safe.

## Implementation Evidence

- `implement` session: `/root/ticket_218_worker`
- `tdd` used: yes
- Red test, if applicable: Product Client assertions initially observed missing
  role/readiness fields and no receipt-presentation interface.
- Green implementation, if applicable: Product Client tests cover complete,
  partial, stale, repair, malformed response, and transport uncertainty; the
  signed server test covers authoritative initial, repaired, rotated, and
  privacy states.
- Refactor, if applicable: current grants are indexed once for bounded
  readiness projection rather than repeatedly scanning grants per row.
- Commands run:
  - `node finite-brain/crates/finite-brain-server/src/product-client.test.js`
  - `scripts/with-dev-env cargo check -p finite-brain-server --locked`
  - `scripts/with-dev-env cargo test -p finite-brain-server --locked signed_organization_collaboration_is_complete_idempotent_and_partial_safe -- --nocapture --test-threads=1`
  - `scripts/with-dev-env cargo fmt --all --check`
  - `scripts/with-dev-env cargo test -p finite-brain-server --locked`

## Review Instructions

Review only this issue's slice unless you find a severe cross-slice regression.
Keep standards and spec findings separate.

Check:

- Acceptance criteria are met.
- Tests verify behavior through public interfaces.
- No implementation-only tests are masquerading as behavior tests.
- No obvious incomplete work, TODO placeholders, or unrelated changes.
- Relevant test, typecheck, build, or visual verification commands pass.
- Metadata never exposes collaborator grant relationships to non-admins.
- Product output never renders public keys, raw Folder Keys, grant plaintext,
  or provider diagnostics.
- Repair uses the same desired-state contract and does not fall back to the
  role-only admin endpoint.

## Reviewer Output

```text
STANDARDS_STATUS: pass | changes_requested
STANDARDS_FINDINGS:
- ...

SPEC_STATUS: pass | changes_requested | skipped
SPEC_FINDINGS:
- ...
```

## First-Pass Review Resolution

- Standards: changes requested and addressed. The metadata projection is now
  constant-size per collaborator and bounded by the durable member capacity;
  member readiness counts only policy-entitled Folders; repair does not depend
  on display-email availability; rendered DOM coverage proves the safe action.
- Spec: changes requested and addressed. Organization collaboration readiness
  is no longer attached to Personal Brain owner rows, with a rendered
  regression assertion.
- Final independent review: pending after the ticket commit.
