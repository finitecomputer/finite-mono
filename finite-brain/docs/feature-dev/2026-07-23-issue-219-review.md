# Review Packet

## Issue

- Issue: https://github.com/finitecomputer/finite-mono/issues/219
- Slice type: managed skill and cross-surface smoke tracer
- Acceptance criteria:
  - Normal managed-Agent sharing uses the email-first convergent collaboration
    command and checks its typed state.
  - Complete, partial, and indeterminate reports are accurate, retryable, and
    secret-safe, without an ad hoc NIP-05 probe.
  - Low-level permission commands remain documented as advanced and
    insufficient to prove collaboration.
  - Both managed skill/reference copies stay synchronized under static checks.
  - Fresh Alpha/Beta state proves existing restricted read, Beta edit/sync, and
    Alpha sync/read-back.
  - Smoke failures identify a product boundary and artifacts contain no
    secrets.
- Baseline: `b5f8ba9`
- Current diff: `b5f8ba9...working tree`

## Implementation Summary

Managed Agents now receive one unambiguous Organization Brain collaboration
workflow: give `fbrain collaborators ensure-admin` the recipient's canonical
Managed Agent Email and interpret its typed receipt honestly. The required
full-product gate now begins with a deterministic built-CLI Alpha/Beta proof
and validates a small secret-free boundary report before launching its existing
real-Hermes matrix.

## Implementation Evidence

- `implement` session: `/root/ticket_219_worker`
- `tdd` used: yes
- Red test, if applicable: `just skills check` failed for component/canonical
  copy drift and missing command, state, retry, and advanced-primitive markers.
- Green implementation, if applicable: static delivery checks pass and both
  skill/reference trees are byte-identical. The process acceptance test passes
  after Beta read/edit/sync and Alpha sync/read-back, and its emitted report
  passes the standalone safe-schema verifier.
- Refactor, if applicable: none
- Commands run:
  - `just skills check`
  - `diff -ru finite-brain/skills/finitebrain finite-skills/skills/software-development/finitebrain`
  - focused built-process collaboration acceptance with report output
  - standalone smoke report validation
  - Python syntax and Rust formatting checks
  - attempted full `finite-brain-cli` suite (doctest process wedged; see note)

The first full CLI-suite attempt wedged only in a Rust doctest process after
the test binaries and was terminated after six minutes. The focused public
acceptance seam was rerun and passed. The repository-prescribed
`DEVFINITY_READY_TIMEOUT_SECS=1800 just dev smoke` and complete
`just brain-product-matrix` now pass. The matrix run also found and fixed a
local browser-selection defect: an executable PATH shim pointed to a removed
Chromium application, so the helper now prefers a real Playwright browser.
Repeat independent review and PR checks on the pushed commit remain explicit
prerequisites; #219 is not approved or closed yet.

## Review Instructions

Review only this issue's slice unless you find a severe cross-slice regression.
Keep standards and spec findings separate.

Check:

- Acceptance criteria are met.
- Tests verify behavior through public interfaces.
- Normal guidance never composes low-level permission mutations.
- Native email resolution is left to `fbrain`; no public NIP-05 curl appears.
- Typed-state wording cannot overstate partial or indeterminate results.
- Skill and reference copies are byte/logically identical.
- The Alpha/Beta path proves existing restricted content before collaboration,
  recipient read and write, and inviter read-back.
- Smoke artifacts name the failed boundary without retaining commands, output,
  credentials, identity keys, Folder Keys, wrapped events, or grant plaintext.
- No unrelated changes are included.

## Reviewer Output

```text
STANDARDS_STATUS: pass | changes_requested
STANDARDS_FINDINGS:
- ...

SPEC_STATUS: pass | changes_requested | skipped
SPEC_FINDINGS:
- ...
```

## First-Pass Review Resolution

- Standards review passed with no material findings.
- Artifact accuracy: `collaborationState`, independent-home setup, existing
  restricted knowledge, recipient read/edit, and inviter read-back are all
  derived from passed boundaries. A failed artifact no longer claims a later
  fact.
- Holder fallback: partial guidance names a holder only when the receipt
  supplies a safe email. Otherwise it directs another current Folder reader
  who can open the listed Folder to retry, and forbids invented identity.
- Local integration is complete: the services smoke and complete product
  matrix pass. Pushed-commit PR checks and repeat independent standards/spec
  approval remain pending.
