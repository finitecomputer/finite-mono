---
name: publish-web-apps-finite
description: Compatibility router for older requests to publish a web app on Finite. Route all current Finite Sites, finite-sites, fsite, static website publishing work to finite-sites-publishing-finite.
---

# Publish Web Apps Compatibility Router

This name is retained only so older prompts do not activate obsolete runtime
publishing guidance.

Read and follow the sibling `finite-sites-publishing-finite` skill. Current
publishing uses the `fsite` 0.6 Project Repository and Project Site model for
static websites. Backend hosting is a separate service; do not promise app
processes or document rendering through Sites.

Do not expose a Runtime port, edit host networking, or substitute another
Finite command family. If `fsite` is unavailable or too old for the requested
output type, stop and report the missing product capability.
