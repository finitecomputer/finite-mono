# Needs deployment

Status: **OPERATIONAL HANDOFF — NOT DEPLOYMENT AUTHORITY**

This queue records merged work that is not yet known to be released or
deployed. Merging, appearing here, or sharing a source revision does not
authorize a release, production deploy, artifact promotion, or Agent Runtime
rollout. Each mutation still needs Paul's fresh approval and its owning
runbook.

## Queue

| Work | Merged source | Surface | Required next action | Close only after | Status |
|---|---|---|---|---|---|
| AEON worker teardown (`finite-specialization/` tree + clawland manifests) | Draft PR on `retire/aeon-worker-teardown` ([#782](https://github.com/finitecomputer/finite-mono/pull/782); consumer cut #777 is merged) | clawland worker delete + repo tree | Do not merge. Blocked on the HTTP image-request counter gate, not canary health: #777 shipped in a runtime image, that image rolled to lat3, then `finite_specialization_requests_total{capability="image"}` flat for a quiet 45-second window. Scale-to-zero is a separate owner go. | Counter flat after the #777 image is on lat3; worker scaled to zero; #782 merged; the three named credentials rotated (`FC_RUNNER_FINITE_PRIVATE_SPECIALIZATION_WORKER_API_KEY` on lat3/lat4 `runner.env`, clawland worker-token, spark-gateway-token); `scripts/finite-status` recorded. | **DRAFT; BLOCKED ON COUNTER GATE** |
| Hermes sidecar stall wedge (unbounded `reqwest::blocking`) + frozen chat-server room projections | PRs [#765](https://github.com/finitecomputer/finite-mono/pull/765), [#768](https://github.com/finitecomputer/finite-mono/pull/768), and [#770](https://github.com/finitecomputer/finite-mono/pull/770); `main` merges `c94134c7…`, `413702a4…` | Agent Runtime image, existing-Agent rollout, and the lat2 chat-server closure | The whole active fleet is on `2026-08-29.4` (carries the sidecar fixes from `.3` plus the merged chat-authz stack #710/711/712, NIP-98 off): `finite-agent-runtime-2026-08-29.4` = `ghcr.io/finitecomputer/agent-runtime:2026-08-29.4@sha256:79d87f10ffc481c64ba8f53ad6e38574f8df0ef2757abd14c7458b6631a11ef6`, promoted 22:09Z; Core records 30 lat3 + 21 lat4 Runtimes on it. The chat-server closure with #770 is live on lat2: configuration revision `9788a9ad`, `finitechat-server` restarted 22:08:20Z. Do not use `2026-08-29.1` (`de0bc95e`, no #765) or `2026-08-29.2` (published, never registered). Known follow-up: #776's quarantined-room hint livelock (measured 25 fetches/s on one agent post-unfreeze) ships in the next runtime image; per-host fresh-launch pin evidence still to be recorded; the wrapper must address Core on lat2 with lat3/lat4 as first-class `--host` targets (this PR's script change) for every future roll. | Agents consume events and retain their Principal and writable `/data`; quarantined-room livelock stops with #776's image; per-agent repair via the existing diagnose/repair tooling where a room needs it. | **FLEET ON .4; #776 IMAGE PENDING; WRAPPER FIX MERGE PENDING** |
| Finite Sites item 1: truthful publishing, automatic viewing, and human sharing | PRs [#194](https://github.com/finitecomputer/finite-mono/pull/194), [#195](https://github.com/finitecomputer/finite-mono/pull/195), and [#196](https://github.com/finitecomputer/finite-mono/pull/196); `main` merge `a912cd5159c25c5fca9c61913c86a26a7c2525da` | `fsite` component release | Cut the next `fsite/vX.Y.Z` tag from an accepted `main` revision and verify the rolling alias per [release-cli.md](runbooks/release-cli.md). | The versioned release and `fsite-latest` serve matching verified assets, and a field install reports the new version. | **NEEDS RELEASE** |
| Finite Sites item 1: truthful publishing, automatic viewing, and human sharing | Same source set | Sites server (`finitesitesd` on lat2) | Prebuild and deploy an exact accepted `main` revision through [deploy-sites.md](runbooks/deploy-sites.md). | The production edge returns `Cache-Control: no-store` for real HTML and assets; an ordinary v1 → v2 publish/reload returns v2; Git push completion reflects reconciliation; invite email copy is human-first. | **NEEDS SITES DEPLOY** |
| Finite Sites item 1: authenticated requester inference | PR [#196](https://github.com/finitecomputer/finite-mono/pull/196), included in merge `a912cd5159c25c5fca9c61913c86a26a7c2525da` | Agent Runtime image and existing-Agent rollout | Same promoted image as the sidecar row: `2026-08-29.4` `@sha256:79d87f10ffc481c64ba8f53ad6e38574f8df0ef2757abd14c7458b6631a11ef6` (whole active fleet on it: 30 lat3 + 21 lat4). | The exact image digest is recorded; a cached second Finite turn initializes and shares every declared output with its authenticated requester; a non-Finite, expired, internal/background, restarted, or mismatched turn does not infer one; each upgraded Agent retains its Principal and writable `/data`. | **IMAGE PROMOTED; FLEET ROLLED 2026-08-29** |
| finite-lat-2 emergency cutover, stage 1: wipe + capture + chassis artifact (ADR 0007) | The replacement scaffold PR (app-plane host config, contracts, `Lat2 NixOS Closure` workflow, capture script, runbooks) once merged | lat2 host | Wipe, capture, and closure artifact already used for the live install. | Capture verified (2+2 NVMe shape fits pinned geometry), captured rev merged on `main`, CI artifact built. | **DONE 2026-08-29** |
| finite-lat-2 emergency cutover, stage 2: install + import state | Stage 1 closed | lat2 host | Install and import completed; Core and product units are live on lat2. | `[UU]` arrays with `finite-storage-health` green, ESP guard intact, all offline verifications pass. | **DONE 2026-08-29** |
| finite-lat-2 emergency cutover, stage 3: go live | Stage 2 closed | lat2 host, lat3 WG peer, DNS | Gate E DNS is live: `finite.computer` → `64.34.80.19`, site 200, reservation route returns Core 422, `finite-saas-core` active on lat2. Remaining: post-cutover cleanup (unregister old GitHub runners, rotate legacy credentials, clear the stale smoke row so fleet aggregate is truthful). | Full loopback verify passes under the go-live closure; every product name serves from lat2; lat3 handshake current; `scripts/finite-status` green. | **GO-LIVE DONE 2026-08-29; NEEDS POST-CUTOVER CLEANUP** |
| finite-lat-4 → third NixOS Runner host (ADR 0007 model): verify + artifact | The lat4 scaffold PR (`infra/nixos/hosts/finite-lat-4/`, `Lat4 NixOS Closure` workflow, capture script, runbook) once merged, and after PR #715 merges | lat4 host | Gate A (`runbooks/lat4-nixos-runner-install.md`): SMART pre-flight on all four NVMe devices, re-run `infra/nixos/scripts/capture-lat4-host-evidence`, provider console confirmation. Gate B: dispatch the `Lat4 NixOS Closure` workflow at the merged rev; `just lat4-runner-rollout-contract`. | Four SMART PASSED, capture diff clean against `docs/runs/lat4-provisioning-prep.md`, and the `lat4-nixos-closure-REV` artifact exists for the merged rev. | **DONE 2026-08-29** |
| finite-lat-4 → third NixOS Runner host: install + drained bring-up | Same scaffold PR, after the Gates A/B row closes | lat4 host | Gate C and Gate D are live: `/etc/finite/runner.env` present, `FC_RUNNER_SOURCE_HOST_ID=finite-lat-4`, overlay `10.254.3.4`, 21 active Runtimes serving. | `[UU]` arrays with `finite-storage-health` green, ESP guard intact, rollback boundary proven, authenticated drained lease, `finite-lat-4` visible in dashboards with `role="runner"`. | **DONE 2026-08-29** |
| finite-lat-4 admission decision (undrain), optional | Gate D evidence | Core keyring + lat4 `runner.env` | Taken: `FC_RUNNER_DRAIN=false` on lat4; 21 active Runtimes already bound there. | 42-guest capacity reported and creation leases beside lat3 (or the decision not to open is recorded in the prep run doc). | **DONE 2026-08-29** |
| finite-lat-4 fleet adoption (lat1 agent state import) | Gates C/D complete + the three state archives, manifest bundle, and four manifests in `/data/staging/` | lat4 host + Core on lat2 | Import and the active lat1 cohort are already on lat4 (20×`2026-08-29.1` + Waffle Prime on `.3`). Seven inactive lat1 rows remain and are different historical projects, not that cohort. Remaining Gate F work is the recorded leftover intersection plus any still-unrelocated named Runtime, one `runtime-cold-relocate-exact` transaction each. Do not run beside the `.3` fleet roll. | All hashes clean, migrated agents answer chat from lat4, Core shows the adopted Runtimes under `finite-lat-4`; source archive intact. | **ACTIVE FLEET ON LAT4; LEFTOVER LAT1 RECORDS + ANY REMAINING EXACT RELOCATIONS** |

No dashboard source changed in these PRs, so this work needs **no dashboard
rollout**.

## Queue discipline

- Add only merged source. Record the PRs and exact merge revision.
- Split rows by independently deployable surface. A coupled NixOS switch may
  satisfy more than one row, but it does not imply an Agent Runtime rollout.
- Replace a status only with immutable evidence: release tag, image digest,
  deployed Git revision/system closure, and the owning runbook's verification.
- Once every row for a work item is closed, record anything the sources of
  truth cannot express in [`deployment-changelog.md`](deployment-changelog.md)
  or the relevant production baseline, and remove the item from this queue.
