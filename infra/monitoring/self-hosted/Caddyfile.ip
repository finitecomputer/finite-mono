http://{$MONITORING_IP} {
	@remote_write {
		method POST
		path /api/v1/write
	}

	# Never send production credentials over the temporary plaintext endpoint.
	handle @remote_write {
		respond "TLS required" 426
	}

	handle {
		reverse_proxy grafana:3000
	}
}
