export default function IOSAuthCallbackPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md items-center px-6 text-center">
      <div>
        <h1 className="text-2xl font-semibold">Return to Finite Chat</h1>
        <p className="mt-3 text-sm text-muted-foreground">
          If the app did not reopen automatically, return to Finite Chat and
          start sign in again.
        </p>
      </div>
    </main>
  );
}
