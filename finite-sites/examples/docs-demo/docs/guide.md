# Guide

Document Outputs use ordinary Markdown files.

- The browser serves rendered pages.
- Appending `.md` returns the exact authored Markdown for a page.
- `/llms.txt` gives agents the Project Repository handoff.
- `/llms-full.txt` gives a bounded authored Markdown snapshot.

