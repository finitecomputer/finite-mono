#!/usr/bin/env bash
set -euo pipefail
repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"
# Always select the repo-pinned Rust/WASM target, Clang and matching bindgen CLI.
exec nix develop .#chat-wasm -c python3 scripts/finitechat-wasm-spike.py "${@:-up}"
