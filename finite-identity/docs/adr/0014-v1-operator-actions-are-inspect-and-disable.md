# V1 operator actions are inspect and disable

V1 operator capabilities are limited to read-only inspection and disabling abusive or unsafe bindings. Operators can inspect Finite VIP Email, NIP-05 Name, Principal Link, and Email Challenge audit metadata, and can create a Disabled Binding, but they cannot reassign a name, rotate a key, migrate product data, or perform Recovery.
