# Non-custodial Local Identity Keys

In v1, the Identity Authority never generates, stores, or returns a user's secret Nostr key material. Local Finite Identity clients generate, import, and store the Local Identity Key, while the Identity Authority accepts public keys and verified email proofs to maintain public identity state such as Principal Links and NIP-05 Names.
